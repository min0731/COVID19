1. 변수 수집(크롤링 등)과 전처리

2. 변수 전처리와 최종 변수 선택

3. 예측 모델(딥러닝 기반 LSTM)학습과 확진자 예측

4. many -to -one  LSTM을 구현한 RNN_based.py 모듈

위 4가지에 해당하는 코드를 보내드립니다.

jupyter notebook 혹은 jupyter lab으로 .ipynb파일을 열어보시면, 실행하지 않아도 결과가 보이도록 노트북 내에 plot과 예측 결과 등을 출력 해 두었습니다. 

단 RNN_based.py 모듈은ipynb파일과 같은 디렉토리에 있어야 합니다.

그 외 numpy, pandas, pytorch 등의 라이브러리 버전은 소스파일 상단에 명시 해 두었습니다.